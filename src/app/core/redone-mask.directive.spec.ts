import { RedoneMaskDirective } from './redone-mask.directive';

describe('RedoneMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new RedoneMaskDirective();
    expect(directive).toBeTruthy();
  });
});
